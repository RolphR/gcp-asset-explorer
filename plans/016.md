# Plan: Update Asset Details Panel and Graph Zoom Behavior

1. **Graph Zoom Preservation**:
   - In `frontend/src/components/GraphViewer.tsx`, modify the `useEffect` that runs when `selectedNodeId` changes.
   - **Remove** the line `fgRef.current.zoom(4, 800);`.
   - By only retaining `fgRef.current.centerAt(node.x, node.y, 800);`, the camera will smoothly pan to the new asset without changing the user's current zoom level.

2. **Display Names for References**:
   - In `frontend/src/App.tsx`, create a helper function that resolves a display name given a node ID:
     ```tsx
     const getNodeDisplayName = (id: string) => {
       if (!data) return id;
       const n = data.nodes.find(node => node.id === id);
       return n?.displayName || id;
     };
     ```
   - Pass `getNodeDisplayName` down to the `Sidebar` component as a new prop.

3. **Asset Details Panel UI**:
   - In `frontend/src/components/Sidebar.tsx`, add `getNodeDisplayName: (id: string) => string;` to `SidebarProps`.
   - Find the rendered lists for both the **Parent** and **Related Assets** properties.
   - For each reference button, set the `title` attribute to the original ID (so it shows up as hover alt text).
   - Change the button text content to the result of `getNodeDisplayName(id)`, displaying the human-readable name instead of the raw GCP ID string.
