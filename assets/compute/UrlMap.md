# Instructions for compute.googleapis.com/UrlMap

## Parent

Add parents based the following rules:

- Any reference in any `backendServices` property.
- Any reference in any `defaultService` property.
- Any reference in any `service` property.
- If none of the above apply, use the default parent instead.

This asset can have more than 1 parent.
Remove any duplicates from the list.
