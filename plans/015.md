# Plan

I have successfully updated the parser and UI based on your instructions!

Here's what I changed:
1. **Parser Changes**: Added the newly ignored fields (`assetType`, `location`, `region`, and `zone`) to the `ignoredKeys` Set inside `getRelatedAssets` in `frontend/src/utils/parser.ts`.
2. **UI Updates**: Modified `frontend/src/components/Sidebar.tsx` so that references listed in the `Parent` field now render as an interactive, clickable list (using `onSelectNode`), matching the behavior of the `Related Assets` panel. 
3. **Validation**: Ran `npm run build` to guarantee everything is typed correctly and compiles smoothly.
